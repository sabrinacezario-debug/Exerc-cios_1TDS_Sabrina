#include <stdio.h>

int main() {
    int n;
    int i;
    int j;

    printf("Digite o numero de linhas para a piramide: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {

        for (j = 1; j <= n - i; j++) {
            printf(" ");
        }

        for (j = 1; j <= 2 * i - 1; j++) {
            printf("*");
        }

        printf("\n");
    }

}
